import 'package:flutter/material.dart';

class MenuReviewer extends StatelessWidget {
  const MenuReviewer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('レビュアー'),
      ),
    );
  }
}

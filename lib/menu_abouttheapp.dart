import 'package:flutter/material.dart';

class MenuAbouttheapp extends StatelessWidget {
  const MenuAbouttheapp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('アプリについて'),
      ),
    );
  }
}